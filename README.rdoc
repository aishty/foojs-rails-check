To test if the gem basicgem_js (https://github.com/aishty/basic-gem) works
1) Install gem locally.
2) Clone this repository, run bundle update and bundle install.
3) Run it on the local server. You should be able to see "Hello world!" on your Javascript console, which shows the gem works.
